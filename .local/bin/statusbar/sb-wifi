#!/bin/sh

case "$(cat /sys/class/net/w*/operstate 2>/dev/null)" in
	up) text="WiFi" ;; # wifi connected
	down)
		case "$(cat /sys/class/net/w*/link_mode 2>/dev/null)" in
			0) text="- -" ;; # wifi disconnected and disabled
			1) text="---" ;; # wifi disconnected
		esac
esac

printf "W$text\n"