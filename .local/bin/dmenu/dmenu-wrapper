#!/bin/sh

# ensures dmenu is run with correct font and theme for custom scripts
# use "dmenu-wrapper" instead of regular "dmenu" command 

. "$THEME"

dmenu -fn 'monospace:size=12:antialias=false:autohint=false' -nb "$BG" -nf "$FG" -sb "$BG_ALT" -sf "$FG_ALT" "$@"
